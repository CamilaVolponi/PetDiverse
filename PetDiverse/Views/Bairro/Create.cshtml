@model PetDiverse.Models.BairroViewModel

@{
    ViewData["Title"] = "Criar bairro";
}

<h2>Criar bairro</h2>

<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="row">
                <div class="col-md-9">
                    <div class="form-group row">
                        <label asp-for="Nome" class="col-sm-5 control-label"></label>
                        <div class="col-sm-7">
                            <input asp-for="Nome" class="form-control" />
                            <span asp-validation-for="Nome" class="text-danger"></span>
                        </div>
                    </div>
                </div>
            </div>
            <br />
            <div class="row">
                <div class="col-md-9">
                    <div class="form-group row">
                        <label asp-for="IdCidade" class="col-sm-5 control-label"></label>
                        <div class="col-sm-7">
                            <select asp-for="IdCidade" class="form-control" asp-items="ViewBag.IdCidade"><option value="">Selecione uma cidade</option></select>
                            <span asp-validation-for="IdCidade" class="text-danger"></span>
                        </div>
                    </div>
                </div>
            </div>
            <div class="form-group mt-2">
                <a asp-action="Index" class="btn btn-cinza">Voltar a lista</a>
                <input type="submit" value="Criar" class="btn btn-roxo" />
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
